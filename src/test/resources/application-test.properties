#
# Copyright (c) TIKI Inc.
# MIT license. See LICENSE file in root directory.
#

# Spring Datasource
spring.datasource.username=${TST_SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${TST_SPRING_DATASOURCE_PASSWORD}
spring.datasource.url=${TST_SPRING_DATASOURCE_URL}

# JWT
com.mytiki.ingest.jwt.bouncer.public_key=${JWT_BOUNCER_PUBLIC_KEY}

# KGraph
com.mytiki.kgraph.url=${KGRAPH_URL}
com.mytiki.kgraph.api_key.value=${KGRAPH_API_KEY}
